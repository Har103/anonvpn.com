generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Order
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          order
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        id:
          help: 2CO order id. Id numbers less than 65536 are reserved for internal use.
      list:    ~
      filter:  ~
      form:    ~
      edit:    ~
      new:     ~
