Ip:
<?php
// server count in Server.yml
$servers = 1;

for ($i = 0; $i < $servers; $i++)
{
  for ($j = 0; $j < 100; $j++)
  {
    printf(' Ip_%u%u:'        . PHP_EOL, $i, $j            );
    printf('  Server: Server_%u' . PHP_EOL, $i                );
    printf('  ip: 10.58.%u.%u'   . PHP_EOL, 1 + $i, 100 + $j  );
  }
}